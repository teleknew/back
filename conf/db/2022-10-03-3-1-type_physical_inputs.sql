CREATE TABLE interface.type_physical_inputs (
                                                id serial4 NOT NULL,
                                                description varchar(20) NULL,
                                                CONSTRAINT type_physical_inputs_pk PRIMARY KEY (id)
);