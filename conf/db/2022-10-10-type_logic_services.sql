CREATE TABLE interface.type_logic_services (
                                               id serial4 NOT NULL,
                                               "name" varchar NULL,
                                               description varchar NULL,
                                               CONSTRAINT type_logic_services_pk PRIMARY KEY (id)
);